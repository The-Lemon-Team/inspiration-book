{
  "name": "inspiration-book-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Inspiration Book — Electron desktop client",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "npm run build && electron .",
    "start": "npm run build && electron ."
  },
  "devDependencies": {
    "electron": "^33.2.0",
    "typescript": "^5.7.3"
  }
}
